// Generated by CoffeeScript 1.3.1
(function() {
  var create, generateId, getLargestId, loadTasks, store, taskToObject, tasksExist;

  $(function() {
    var textfield;
    console.log("in todoapp");
    $("#tasks").sortable();
    textfield = $("#todotext");
    return $("#todotext").keydown(function(e) {
      var code, id, rank, text;
      code = e.keyCode;
      if (code === 13) {
        text = $("#todotext")[0].value;
        console.log("Task: " + taskText);
        $("#todotext")[0].value = "";
        id = getNewId();
        rank = getNewRank();
        return create(id, rank, text);
      }
    });
  });

  loadTasks = function() {
    var ids, tasks;
    if (!tasksExist()) {
      return;
    }
    $("#taskList #title").show();
    ids = Object.keys(localStorage);
    tasks = _.map(ids, function(id) {
      var taskObj, taskString;
      taskString = localStorage.getItem(id);
      taskObj = JSON.parse(taskString);
      return taskObj;
    });
    return _.each(tasks, function(taskObj) {
      return create(taskObj.id, taskObj.rank, taskObj.text);
    });
  };

  create = function(id, rank, text) {
    var task;
    task = $("<li></li>").clone();
    task.data("id", id);
    task.data("rank", rank);
    task.data("text", text);
    task.appendTo("#tasks");
    return store(task);
  };

  store = function(taskJSON) {
    var id, task;
    id = taskJSON.id;
    task = JSON.stringify(taskJSON);
    return localStorage.setItem(id, task);
  };

  taskToObject = function(task) {
    var json;
    json = {
      "id": task.data("id")({
        "rank": task.data("rank"),
        "text": task.data("text")
      })
    };
    return json;
  };

  generateId = function() {
    return getLargestId() + 1;
  };

  getLargestId = function(keys) {
    return _.max(keys);
  };

  tasksExist = function() {
    var ids;
    ids = Object.keys(localStorage);
    if (!_.isEmpty(ids)) {
      return true;
    } else {
      return false;
    }
  };

}).call(this);
